enum Flavor { DEV, STAGE, PRODUCTION, BETA }

enum MedSosType { TWITTER, IG, FB }

enum TextFieldType { FREE_TEXT, PASSWORD }

enum dashboardType { DEMO, NORMAL }

enum TicketType { open, progress, closed, pending, escalated, spam }

enum ChatType { TEXT, AUDIO, FILE, IMAGE, VIDEO }

enum Menu { logout }

enum MenuRoomCloseType { history, category }

enum MenuRoom { history, category, close }

enum CategoryType { tag, category }

enum ProfileTag { select, input, textarea }

enum ProfileType { select, text, textarea, number, email }
